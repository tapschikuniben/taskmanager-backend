# taskmanager-backend
Secure Spring Boot RESTful API backend for a single user. It handles all core business logic and provides authenticated endpoints for managing tasks. Data is persisted efficiently using an in-memory H2 database. Built for easy deployment and high performance.
